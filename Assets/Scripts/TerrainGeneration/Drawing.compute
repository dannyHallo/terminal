RWTexture2D<float4> universalRenderTex;
int radius;
float4 drawColor;
int2 origin;

// k0: Draw on texture
#pragma kernel CSMain

[numthreads(8,8,1)]
void CSMain (uint2 id : SV_DispatchThreadID)
{
    if(distance(id, origin) <= radius){
        universalRenderTex[id.xy] = drawColor;
    }
}
